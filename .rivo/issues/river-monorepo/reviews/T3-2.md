# T3：修复复审（第 2 轮）

- **方式／审阅者**：修复复审，Claude（子代理）。没有派发下级代理，没有修改实现。变异验证直接改工作区的 `packages/ui/src/fx.ts`，跑完后用 `git checkout` 恢复，已确认工作区只剩无关的未跟踪文件 `.rivo/issues/river-desktop/reviews/final-1.md`。
- **范围**：前轮 `reviews/T3-1.md` 的 F1–F6；修复提交 a31d4f5 的全部改动（`packages/ui/test/table-stage.test.tsx` +8 行，`plan.md` 一行，以及把 T3-1.md 提交入库）。
- **代码版本**：实现 dc40968，修复 a31d4f5（HEAD，分支 feat/monorepo-site-i18n）。
- **需求版本**：当前工作区中的 task.md、plan.md、ADR-007。

## 结论

**F1、F5 已关闭，代码侧可以完成 T3；F4 仍是证据缺口，T3 的“结果”要等主代理开桌实测后才能写完。** 新用例能检出前轮存活的变异 M1，主代理的说法我复现了。F2 的去向合理，但 task.md 的 T8 里还没有写进去，需要补一笔（见 N1）。

## 判断

- **F1 的新用例覆盖了验收里的两种情况**：同一座位 `bet: 40, lastAct: 'raise'` 保持不变，status 先变成 `Thinking…`（加注者再次思考），再变成 `Won 80`（加注后赢下），每一步都断言特效层里没有该文字。这正是“status 变了、lastAct 仍是 raise、下注额没增加”的组合，只有 `cur.bets[i] > P.bets[i]` 能拦住。
- 用例用英文 status，和前面几条用例的写法一致；ui 不含文案，status 用什么语言与被测逻辑无关。
- 验收里“玩家本人加注后又轮到自己”没有单独写用例。它同样属于“下注额没增加”，被同一个条件拦住，新用例已经守住这个条件，不再另外要求。
- **F5 的 plan 措辞**准确，并且注明了出处（T3 审阅 F5），与前轮推荐的做法一致。
- a31d4f5 没有改 `src/`，实现部分沿用前轮结论。

## 发现

| 编号 | 类型 | 具体影响 | 依据与位置 | 建议 |
|---|---|---|---|---|
| N1 | 待对齐（记录缺失） | 前轮 F2 的结论是“写进 T8”，但 task.md 的任务 8 还没有这条要求（第 490 行只写了外层 `transform: scale`）。T3 的“结果”还空着，如果到时忘了转录，T8 的执行者不会知道舞台根节点有 `min-h-[560px] flex-1` 和固定底色 | task.md 任务 8“实施步骤”第 3 步；reviews/T3-1.md F2 | 写 T3“结果”时，在任务 8 第 3 步或“工程注意事项”里加一句：外层要给定宽度、高度不低于 560px，确认舞台底色是否可以接受；不能接受时再给 `TableStage` 加可选 `className`（用 `cn` 合并），并更新 plan 的 props 表 |

没有发现新的实现问题。

## 旧发现处理结论

| 编号 | 处理 | 结论 |
|---|---|---|
| F1 | a31d4f5 新增用例“下注额没增加时不播加注特效” | **已关闭**。我自己去掉 fx.ts 第 210 行的 `cur.bets[i] > P.bets[i] &&`，新用例在 `Thinking…` 这一步失败，其余 4 条通过；恢复后 5 条全部通过 |
| F2 | 延后到 T8 | **去向合理**：T3 先不加 prop，避免预支需求。但转录还没做，见 N1 |
| F3 | 可选，未处理 | **可以接受**。误差不超过 0.5px，看不出来。plan 第 237 行“系数为 1”的措辞仍不严格，可以顺手改成“k≈1”，不阻塞 |
| F4 | 待用户同意模型费用后由主代理开桌 | **仍是证据缺口**。本轮没有开桌，也没有调用模型。完成 T3 前还需要：特效逐项核对（含 `lite`、`off`）、牌桌截图对比、设置页“桌面外观”色块截图，同时补上 T2 F4 |
| F5 | plan.md @river/ui“原则”注明 `ALL IN`、`D` 是视觉符号 | **已关闭** |
| F6 | 可选，未处理 | **可以接受**。`--topbar` 留在共享 tokens 里，官网用不到，不影响功能 |

## 实际验证

| 命令／操作 | 结果 |
|---|---|
| `git show a31d4f5` | 只改了测试、plan 一行，另外把 T3-1.md 提交入库；`src/` 没有改动 |
| 用 sed 去掉 `packages/ui/src/fx.ts:210` 的 `cur.bets[i] > P.bets[i] &&`，`git diff` 确认只改了这一行，然后在 packages/ui 跑 `pnpm test` | 1 failed / 4 passed，失败的是新用例：`expected 'Thinking…' not to contain 'Thinking…'`（test:64） |
| `git checkout packages/ui/src/fx.ts`，然后 `git status --short` | 只剩 `?? .rivo/issues/river-desktop/reviews/final-1.md` |
| packages/ui 下 `pnpm test` | 5 passed |
| packages/ui 下 `pnpm typecheck` | exit 0 |
| grep task.md、plan.md 里的 F2、F3 相关记录 | task.md 任务 8 还没有 F2 的要求；plan 第 237 行仍写“系数为 1” |

没有跑根目录的全量 test、typecheck、build（a31d4f5 没有改源码，前轮在 dc40968 上已全部通过）。没有启动 `pnpm dev`，没有开牌局。

## 后续

- **T3 完成前**：主代理在用户同意费用后开桌，补齐 F4；写 T3“结果”时把 F2 转录进任务 8（N1）。
- **可选**：F3 修正 plan 措辞；F6 把 `--topbar` 移回 desktop。
- **与 T3 无关**：`.rivo/issues/river-desktop/reviews/final-1.md` 仍是未跟踪文件，前轮已提醒主代理确认 e9e5779 中的删除是否有意。
