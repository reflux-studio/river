# T5 审阅（第 2 轮）：修复复审

## 方式、范围与版本

- 模式：修复复审。我是独立审阅者，没有派发子代理，除本报告外没有修改任何文件。
- 范围：只审修复提交 `202170a`（父提交是 T5 实现 `d172b9b`），对照前一轮报告 `reviews/T5-1.md` 的 F1–F5。工作区里未提交的改动（执行者正在同时修改渲染进程）没有参与判断。
- 版本：用 `git worktree add --detach <scratchpad>/wt 202170a` 检出独立工作树，所有验证都在这个工作树里完成，用完已执行 `git worktree remove`。
- 已读：`git show 202170a` 的全部 7 个文件。其中 `reviews/T5-1.md` 和 `evidence/T5/dev-lobby.png` 是上一轮的产物，只是一并提交。

## 判断

### 改动内容：符合要求，没有超出范围

- **F1**：`i18n.test.ts` 中 `strings()` 遇到函数词条时，先用真值参数 `['a','b','c','d','e']` 调用一次，再用假值参数 `[0,'','','','']` 调用一次，两次都经过 `call()`（带 try/catch），结果再递归收集。
- **F4**：`en.desktop.model.me` 中的 `current hand` 改为 `current made hand`，只改了这一处。
- **F5**：`promptZh.limits` 新增 `digest: 60`，`promptEn.limits` 新增 `digest: 180`；`runner.writeCoach` 中的 `text.slice(0, 60)` 改为 `text.slice(0, d.prompt.limits.digest)`，其中 `d = tr()`，与同文件其他 `limits.*` 的取法一致。`limits` 注释相应更新。
- **task.md**：T5 的“结果”部分补写了实现提交、F1/F4/F5 的修复、审阅通过，以及 F2/F3 的去向；T6 的注意事项新增一条：教练面板和 `ProbPanel` 的牌型改用 `t.poker.hand[nums.handCat]`。
- 代码改动只涉及 4 个文件，共 +16/−7 行，没有顺带做其他改动。

### 中文行为：不变

- 中文 `limits.digest = 60`，等于 `d172b9b` 中写死的 `60`。`tr()` 定义为 `dict(settingsCache.locale)`（`runner.ts:44`），中文模式下取到的就是 `promptZh`，所以教练往手摘要的截断结果与之前逐字相同。
- `current made hand` 只改了 `en`，中文 `me` 词条没有变。
- 其余 `limits.say` 和 `limits.note` 的 4 处调用点（`runner.ts:294/295/498/614/621`）都没有改动。

### F1 修复的有效性

- 英文字典加预设中一共有 51 个函数词条。我用两组参数逐个调用，全部返回字符串，没有一个抛出异常。
- `dict.ts` 和 `prompts.ts` 中的分支全部是按真假判断的三元表达式，没有 `===`、比较大小、`.map`、`.join` 之类的分支。所以“真值一次、假值一次”能覆盖所有分支里的固定文字。

## 发现

### 旧发现处理结论

| 编号 | 结论 | 依据 |
| --- | --- | --- |
| F1 | **关闭** | 新的 `strings()` 同时检查函数两条分支中的文字；我做了变异验证，见“实际验证”第 3 条。原报告提到的 `heroView` 没有分支，本来就会被新测试检查到，直接调用 `dict('en')` 全部词条的单元测试覆盖面比压测更全。 |
| F2 | **已转 T6** | `task.md` 的 T6 注意事项已写明教练面板和 `ProbPanel` 改用 `t.poker.hand[nums.handCat]`，替换 `dict('zh')`。 |
| F3 | **交接说明** | 按原建议保持现状。`task.md` 的 T5 结果已写明“截断长度按语言区分：中文不变，英文放宽，交接时向用户说明”。新增的 `digest` 也属于 `limits`，与 F3 的处理口径一致。 |
| F4 | **关闭** | 已改为 `current made hand`，不再与 `hand N` 撞词。 |
| F5 | **关闭** | 截断长度改为按语言取 `limits.digest`，英文为 180，与 say/note 的 120 处在同一量级（摘要比单句长，取值合理）；中文为 60，不变。 |

### 本轮新发现

| 编号 | 类型 | 具体影响 | 依据与位置 | 建议 |
| --- | --- | --- | --- | --- |
| N1 | 可选改进（极低） | `call()` 的 try/catch 会把抛出异常的函数词条变成 `undefined`，这个词条就会被跳过，而且不会报错。目前 51 个函数词条都不会抛出，所以这段捕获是没有用到的代码。以后如果有词条需要数组或对象参数（例如调用 `.map`），它会从检查中悄悄消失。 | `packages/i18n/test/i18n.test.ts` 的 `call` | 可以删掉 try/catch，让异常直接暴露；也可以保持现状。不阻塞。 |

没有发现实现缺陷。

## 实际验证

1. **安装依赖**：在工作树中运行 `pnpm install --offline --frozen-lockfile`，完成，desktop 的 postinstall 也跑完了。
2. **根目录 `pnpm typecheck` 和 `pnpm test`**：
   - typecheck：engine、i18n、ui、desktop 都显示 Done。
   - test：engine 36、i18n 4、ui 5、desktop 110，全部通过，与上一轮数量相同。
3. **变异验证（只改动假值分支中的英文文字）**：每次只改一处，跑完后恢复，结果如下：

   | 变异 | 位置 | 结果 |
   | --- | --- | --- |
   | `'no showdown'` → `'未摊牌'` | `handHead` 的假值分支 | 测试失败 |
   | `'Free game'` → `'自由局'` | `seated` 的第 4 个参数的假值分支 | 测试失败 |
   | `'lost'` → `'输'` | `result` 的假值分支 | 测试失败 |
   | `'The player’s'` → `'玩家'` | `me` 的假值分支（复现主代理的变异） | 测试失败 |
   | 旧版 `strings()`（取自 `d172b9b`）加上 `'no showdown'` → `'未摊牌'` | 对照组 | 测试**通过**，说明旧测试确实漏检，修复是有效的 |

   四组变异都是“无汉字”这一用例失败。每次恢复后 `git status` 都是干净的，复跑 4/4 通过。
4. **函数词条探针**：用一个临时测试遍历 `dict('en')` 和 `presets.en`，结果是 51 个函数、0 个抛出异常、0 个返回非字符串。临时文件已删除。
5. **分支形态检查**：对 `dict.ts` 和 `prompts.ts` 做 grep，没有 `===`、`!==`、比较大小、`.length`、`.map`、`.join`、`if` 或 `switch` 这类分支，全部是按真假判断的三元表达式。
6. **调用点检查**：`limits.*` 在 main 中的调用点一共 6 处，都是从 `d.prompt.limits` 取值，已经没有写死的截断长度。
7. **清理**：执行了 `git worktree remove`，`git worktree list` 中只剩主工作树。

## 后续

- T5 修复复审**通过**：F1、F4、F5 关闭，F2 已转 T6，F3 在交接时向用户说明。
- N1 是可选的小改进，不阻塞，由实施方决定是否处理。
- 证据限制：这次只修改了字典和测试，runner 中也只改了一处截断长度，没有重跑英文压测和 dev 实机。中文行为不变的结论是根据取值等价（60 → `promptZh.limits.digest = 60`）静态推出的，没有重做上一轮的 461 次调用逐字对比。
