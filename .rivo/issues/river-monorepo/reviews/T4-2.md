# T4：修复复审（第 2 轮）

- **方式／审阅者**：修复复审，Claude（子代理）。没有派发下级代理，没有修改实现或契约（只写本报告）。验证在 scratchpad 的临时 worktree 中进行，没有碰主工作区（另一执行者正在改 T5）和用户的 River-dev 数据。
- **范围**：只看提交 0dd2247 本身（`git show 0dd2247`）：`apps/desktop/src/main/db/index.ts`、`apps/desktop/test/db.test.ts`、`plan.md`、`task.md`，以及随提交入库的 `reviews/T4-1.md`。
- **代码版本**：0dd2247（父提交 07c48e5）。
- **需求版本**：上一轮 `reviews/T4-1.md` 的 F1–F3、E1–E2；task.md“任务 4”“任务 6”；plan.md“@river/i18n 与 desktop 双语”。

## 结论

**F1、F3 已关闭；T4 可以完成，没有新的阻塞项。** 红绿都已实测：改回 `in` 后新用例失败，而且失败方式正是 F1 担心的那种（校验被绕过，走到加载预设才崩）；恢复 `Object.hasOwn` 后通过。task.md 的新增内容与 plan 一致，只剩 T4 原任务描述里两处“context”旧措辞（N1，不阻塞）。

## 判断

### F1 修复

- `if (!Object.hasOwn(presets, locale)) throw ...`：只认 `presets` 的自有键，`'constructor'`、`'toString'` 这类原型链上的键被拒。和 `'zh' | 'en'` 的字面比较相比，它跟随 `presets` 的键，以后加语言不需要改这里，是更好的写法。
- 新用例放在已有的 `'fr'` 断言之后，并且后面紧跟着 `expect(db.getSettings().locale).toBe('zh')`，所以它同时守住“抛错”和“没有写库”。
- `Object.hasOwn` 需要 ES2022 lib，desktop 的 `tsconfig.node.json` 能通过 typecheck（见实际验证）。Electron 自带的 Node 也支持。

### F3 修复

- plan.md 第 293 行改成“`useT()` hook，实现为 `dict(useRiver(s => s.settings.locale))`，不需要 Provider”，与 `lib/river.ts` 的实现一致。plan 中已经没有其他 context 或 Provider 的说法。

### task.md 改动

- T4“结果”：记录了实现提交、F1 的修复方式、T4-1 的结论、F2 转给 T6、F3 已改 plan，与实际情况一致。“F1 修复见后续提交”没有写哈希，提交就是 0dd2247 本身，可以理解。
- T6 工程注意事项：
  - “用 `useT()`（T4 已提供）”：与 plan 一致。
  - “修正英文牌型表第一行超出分隔线（T4 审阅 F2）”：与 T4-1 的 F2 建议一致。
  - “模拟英文系统：dev 启动时加 `-- --lang=en-US -AppleLanguages "(en-US)"`”：与 `evidence/T4/commands.md` 第 34 行实测用过的命令一致（`npx electron-vite dev ... -- --lang=en-US -AppleLanguages "(en-US)"`）。见 N2。
- 没有和 plan 或其他任务冲突的新内容。

## 发现

| 编号 | 类型 | 具体影响 | 依据与位置 | 建议 |
|---|---|---|---|---|
| N1 | 文档残留（轻微） | T4 原任务描述里还有两处旧措辞：“`lib/river.ts` 提供 i18n context”“提供一个 `useT()` context”。plan 已改，T4 的结果和 T6 也都写的是 hook，所以不会误导后续任务。只是同一个文件里前后不一致 | task.md 第 251、298 行 | 可选：改成“`useT()` hook”。不阻塞 |
| N2 | 文档精度（轻微） | “dev 启动时加 `-- --lang=...`”没有说明是在哪条命令后面加。证据里实测的是在 `apps/desktop` 下直接跑 `npx electron-vite dev`。如果从根目录跑 `pnpm dev`，参数要多经过一层 pnpm 转发，分隔符的写法可能不同，T6 的执行者可能要试一下 | task.md 第 407 行；commands.md 第 34 行 | 可选：写全命令，例如 `cd apps/desktop && npx electron-vite dev -- --lang=en-US -AppleLanguages "(en-US)"` |

## 旧发现处理结论

| 编号 | 上轮类型 | 本轮结论 |
|---|---|---|
| F1 | 可选改进 | **已关闭**。改为 `Object.hasOwn`，加了 `'constructor'` 用例，红绿都已实测 |
| F2 | 可选改进 | **转入 T6**，已写入 T6 工程注意事项。T4 不处理 |
| F3 | 待对齐（文档） | **已关闭**。plan 已改为 `useT()` hook；task.md 还有残留，见 N1 |
| E1 | 证据缺口（轻微） | 维持原结论：不阻塞，整体验证时如果有 dev 环境就真点一次遮罩 |
| E2 | 证据说明 | 维持原结论：不阻塞，T5/T6 开桌实测时确认英文预设能入座 |

## 实际验证

- `git worktree add --detach <scratchpad>/wt 0dd2247`，在其中执行 `pnpm install --offline --frozen-lockfile`（3.4 秒）。
- **绿**：`apps/desktop` 下 `npx vitest run test/db.test.ts`：29/29 通过。
- **红**：用 sed 把该行改回 `if (!(locale in presets))`，重跑后 1 个失败：`语言 > ipc：settings.update 拒绝改语言；onboarding.done 转发`，位置是 `db.test.ts:197`（`'constructor'` 那一行），`Expected: "unknown locale"`，`Received: "seeds(...).map is not a function"`。说明使用 `in` 时校验被绕过，`completeOnboarding` 一直执行到按新语言加载预设才崩溃，正是 F1 所说的后果。随后 `git checkout` 还原，`git status` 干净。
- `pnpm typecheck`（worktree 根目录）：i18n、ui、desktop（node 和 web 两份 tsconfig）全部 Done。
- `node -e`：`'constructor' in {zh:1,en:1}` 为 true，`Object.hasOwn(p,'constructor')` 为 false，`Object.hasOwn(p,'en')` 为 true。
- `git worktree remove`，`git worktree list` 只剩主工作区。
- 没有启动 dev，没有调用模型，没有碰主工作区的未提交改动和 River-dev。

## 后续

- T4 可以标记完成。
- N1、N2 可以在下次编辑 task.md 时顺手修改，不必单独提交。
- F2 由 T6 处理，E1、E2 留到整体验证。
