# 修订复审：plan 第 5 轮 + task 第 5 轮

## 审阅对象与依据

| 项 | 位置 |
| --- | --- |
| 方案 | `plan.md` 当前快照（592 行），重点看第 182、190–192、249、375、394、428 行 |
| 任务 | `task.md` 当前快照（511 行），重点看第 125、146、318、365、375–378、419–420 行 |
| 前轮报告 | `reviews/task-4.md`（M1、L-a、L-b） |
| 用户决定 | `discussion.md` §1：赢牌由 agent 说；赢家发言算主动发言，会引起回应 |

## 结论：通过，可以进入 T5 实施

M1、L-a、L-b 都已解决。全文搜索“赢家”“自动下一手”“预设”“熔断”“抢占”后，plan 与 task 之间没有矛盾，两者都符合用户决定。另有 2 条低优先级建议，都可以在实施 T5 时顺手处理，不需要再修订 plan。

## 前轮问题处理情况

| # | 本轮修订 | 结果 |
| --- | --- | --- |
| M1 | plan 182 与 task 376：自动下一手的条件加上“没有进行中的赢家发言”；ask 或赢家发言结束时，如果已满 4.5 秒就立即发下一手，不满就等到 4.5 秒。task 318 加入 `winSpeechInFlight`，task 378 说明等待和过期检查的规则。plan 249 和 task 378 都写明：下一手开始后才返回的赢家发言整体丢弃，不补台词，这种情况只会因玩家手动发下一手或离桌出现。plan 394 把补台词的情形限定为“本手内失败、熔断，或未过期但被丢弃” | 已解决。开启自动下一手时，赢家发言最多让下一手晚开始约 8 秒，而不会在 4.5 秒时被丢掉。“过期丢弃”只剩下用户主动操作这一种来源 |
| L-a | task 365：“教练模型未配置时立即返回……”这句旧文字已删除 | 已解决 |
| L-b | task 125：T3 的修改位置加入 `src/main/index.ts` 中 `setPath` 的最小改动 | 已解决。T3 的 vitest 验收没有覆盖 `setPath`，但它只有一行，T9 打包验收会用到正式目录，不影响执行 |

## 一致性核对

| 主题 | plan | task | 是否一致 |
| --- | --- | --- | --- |
| 赢家发言是否引起回应 | 26、375：会，算主动发言 | 375：成功且有 say 时 `triggers:true` 并调用 `triggerReplies`；420 的测试写“win 触发” | 一致，符合 §1 |
| 何时补预设台词 | 394：本手内失败、熔断，或未过期但被丢弃；192：熔断后改用预设；428：本地引擎用预设 | 375：未过期但失败、LLM 不可用或熔断时用预设；378：`Dropped` 属于“被丢弃” | 一致 |
| 赢家选择不说话 | 未写补台词 | 378：不补 | 一致。plan 394 列出的三种情况都不包括“选择不说话” |
| 过期后整体丢弃 | 249、394 | 378 | 一致，来源都限定为手动发下一手或离桌 |
| 熔断计数 | 190：win 失败不计入 | 375 没有调用 `countFailure` | 一致 |
| 抢占 | 249：下一手决策抢占时，丢弃未开始的赢家发言 | AgentQueue 规则（278–279 行）：抢占时未开始的非 durable 任务变为 `Dropped` | 一致。按修订后的自动下一手规则，这种抢占只会在手动发下一手之后发生 |
| 自动下一手等待 ask | 182 | 365、376、419 | 一致 |

## 低优先级（建议在实施 T5 时处理）

| # | 问题 | 后果 | 建议 |
| --- | --- | --- | --- |
| L1 | `winSpeechInFlight` 是布尔值，task 378 只写了“赢家发言期间置位”，没有写何时、由谁清除。`askInFlight` 用 `askId` 防止旧调用误清除，这里没有对应的保护 | 两种情况会出问题。一是过期或 `Dropped` 的分支漏掉清除，标志一直为 true，此后自动下一手永远不再触发。二是玩家手动发下一手后，旧的赢家发言还在进行，而新的一手很快结束并开始了新的赢家发言；这时旧调用返回并清除标志，新一手会提前自动发牌，新的赢家发言因过期被丢弃。第二种情况很少见 | 仿照 `askId`，记录发言所属的手号，例如 `winSpeechHand = hand`。无论结果如何（成功、失败、`Dropped`、过期），都在 `finally` 中只清除本手的标志，然后检查自动下一手；`nextHand()` 和离桌时把它重置为空 |
| L2 | T5 的验证清单（419 行）只有“自动下一手等 ask 结束”，没有针对 M1 修复的测试 | 如果回归，要到 T9 真实联调才能发现，而且现象时有时无 | 补一条测试：假 agent 的 win 调用延迟 6 秒，开启自动下一手时，赢家发言写入公屏，并且下一手在发言返回后才开始 |

说明：plan 第 3 行和 task 第 5 行的状态和依据仍写“第 4 轮独立审阅通过（reviews/plan-4.md）”。这不影响执行，可以顺手改为本报告。

## 检查限制

只阅读了文档，没有运行代码。
