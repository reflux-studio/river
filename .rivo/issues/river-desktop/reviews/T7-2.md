# 任务审阅：T7 牌桌页（第 2 轮，修复复审）

## 方式、范围与版本

| 项 | 内容 |
| --- | --- |
| 模式 | 修复复审，独立审阅者。没有派发子代理，也没有修改实现 |
| 标准 | 玩具项目，设计稿只作视觉参考；重点看核心逻辑与体验、架构清晰、复用 |
| 依据 | `task.md` 任务 T7、前轮 `reviews/T7-1.md`（F1、O1、O4，以及托管标签重复） |
| 审阅对象 | 主进程已提交到 `c411f1a`，工作区相对它的改动：`src/shared/types.ts`、`src/main/table/view.ts`（`git diff c411f1a -- src/shared src/main`，共 2 行）。渲染层未提交：`components/Segmented.tsx`、`components/table/{ActionBar,CoachPanel,Seat,ChatPanel}.tsx`、`pages/{Table,Replays}.tsx` |
| 证据 | 执行者提供的 `evidence/T7/r2-record.md`、`r2-raise-step.txt`，我看了 `r2-table-1440.png`。我自己跑的命令输出在 scratchpad：`t7r2-{typecheck,build,test}.txt` |

## 结论：通过

前轮 4 项都按建议处理了，没有发现回归。唯一要补的是文档：T7 结果段还没写，写的时候要带上一行“契约补充：`TableView.bb`”。

## 命令结果

| 命令 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过（exit 0） |
| `pnpm build` | 通过（exit 0） |
| `pnpm test` | 10 个文件、111 个用例全部通过（exit 0） |

## 逐项核对

| 项 | 改动 | 判断 |
| --- | --- | --- |
| F1：bb 改为契约字段 | `TableView` 新增 `bb: number`，`buildTableView` 填 `bb: g.bb`。`ActionBar` 改读 `v.bb`，步进和“重新买入 bb×100”都用它。`bbOf`、标题正则和 `bb` prop 已全部删除（grep 没有残留） | 正确。只新增了字段，已有调用方不受影响，typecheck 通过 |
| O1：`Segmented` 复用 | 新增 `full?`，打开时槽位加 `flex w-full`、条目加 `flex-1`。`ToggleGroup` 的默认 `w-fit` 会被 `cn`（twMerge）用 `w-full` 覆盖。教练栏改用 `<Segmented full …>`，手写的 ToggleGroup 已删除，防止取消选择的逻辑也由 `Segmented` 统一处理 | 正确。截图里分段控件撑满了宽度 |
| O1 回归：其他调用方 | `Settings.tsx`（2 处）和 `Lobby.tsx`（2 处）都没有传 `full`。`full && …` 结果为 `false`，`cn` 会把它过滤掉，生成的 class 与之前完全相同 | 外观不变 |
| O4：Replays 复用 `PlayingCard` | `BoardCard` 已删除，公共牌改为 `<PlayingCard card={c} w={50} h={70} />`。字号按牌高缩放（约 20/21px），与之前的写死值接近 | 正确 |
| O4 回归：日期时间 | `Replays.tsx:187` 仍然用 `toLocaleString('zh-CN', {month, day, hour, minute})` 显示 `playedAt` | 已保留 |
| 托管重复标记 | `Seat` 名字旁的“托管”小标签已删除，只保留主进程给出的状态文字“托管 · {行动}”。`AutopilotTag` 移到 `ChatPanel` 作为文件内常量，公屏的 act 行和 msg 行两处都在用 | 正确。标签组件只剩一个使用方，放在那个文件里合理 |
| 加注步进 | −/+ 改成函数式更新 `setRaiseTo((x) => clamp(x ± bb))`，同一次渲染内连续点击会基于最新值累加。`clamp` 仍然夹在 `[L.minTo, L.maxTo]`，发送时 `act` 会再夹一次。函数式更新闭包里拿到的 `L` 就是本次渲染的合法范围，同一次渲染内不会变 | 正确。证据 `r2-raise-step.txt`：300 → 连按 +2 次 → 500 → 连按 −3 次 → 200（被 minTo=200 挡住） |

## 发现

没有新的阻塞项或建议项。

提醒（文档）：`task.md` 里 T7 的结果段还是“（交付阶段记录）”。写结果时请按共同约定记下“契约补充：`TableView.bb`”，也把前轮 O2（熔断状态有两个来源）和 O3（重载后看不到失败的回答）作为已知小问题各记一行。

## 过度设计检查

没有。`full` 是一个布尔参数，只影响两处 class；没有为这一个用处引入通用的 `className` 透传。其他改动都在删代码。
