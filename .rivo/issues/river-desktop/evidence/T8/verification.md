# T8 验证记录（2026-09-24）

环境：`pnpm dev --remoteDebuggingPort 9444`，数据目录 River-dev；通过 CDP 调用 window.river 驱动（本地引擎、speed 2、autoNext 关，结束后恢复原设置）。教练模型未配置。

## 打牌
- 已有 1 手（id 1，+50）；新打 3 手（3 人桌 10/20，阿狸/小白）：第 1 手翻前加注后一路过牌，第 2 手跟注，第 3 手大盲过牌。原始数据见 hands-dump.json。
- 之后在回放页挂载状态下再打 2 手单挑（对石头），验证 hands:changed 刷新：列表由 4 条变为 6 条，详情自动切到最新一手。

## KPI 手工核对
4 手时（stats.png）：
| id | net | showdown | vpip | pfr |
|---|---|---|---|---|
| 4 | −20 | 是 | 否 | 否 |
| 3 | −40 | 是 | 是 | 否 |
| 2 | −60 | 是 | 是 | 是 |
| 1 | +50 | 否 | 否 | 否 |
- 已打手数 4；净盈亏 −20−40−60+50 = −70；赢下 1/4 = 25%；VPIP 2/4 = 50%；PFR 1/4 = 25%；摊牌 3 次，其中 net>0 为 0 → 0%。界面一致。
- 柱状图：mx = 60；高度 +50→63px、−60→76px、−40→50.7px、−20→25.3px，时间顺序从旧到新（截图一致）。

6 手时（kpi-after-6-hands.json、stats-6-hands.png）：净 −70+10+10 = −50；赢下 3/6 = 50%；VPIP 2/6 = 33%；PFR 1/6 = 17%；摊牌 3 次 0%。界面一致。

## 回放
- replays-latest.png：倒序列表、默认选最新；标题/盈亏/盲注·底池·摊牌；公共牌；玩家行（底牌、赢得 60 · 一对、牌型）；按街道分组，发牌行显示公共牌。
- replays-hand2-review-failed.png：阿狸“已弃牌”；无教练模型时按钮禁用并提示“配置教练模型后可用 · 去设置”；直接调用 hands.review 收到 review:done {error:'not_configured'}，卡片显示“复盘失败，稍后再试”。
- 第 1 手（id 1）老K 弃牌显示“已弃牌”，无公共牌时不显示公共牌区。
- 未验证：复盘成功保存与重开仍在（需要真实教练模型，留 T9）；托管标记（需要 LLM 超时，本次数据无托管项）。

## 命令
pnpm typecheck 通过；pnpm build 通过；pnpm test 10 文件 111 用例通过。

## 第 2 轮（修 reviews/T8-1.md F1）
- Stats.tsx、Replays.tsx 的列表加载加递增序号，只接受最后一次发起的加载结果；失败时 toast，已有数据保留，无数据时显示“加载失败，重试”（回放左栏同样在列表上方显示重试）。
- r2-typecheck.txt：通过；r2-test-output.txt：10 文件 111 用例通过；r2-smoke.json：dev 下两页正常渲染（数据目录已含 T7 打的手，共 11 手）。
- 失败分支与乱序分支未在运行时触发（需要让 IPC 失败或延迟），仅代码审查。
