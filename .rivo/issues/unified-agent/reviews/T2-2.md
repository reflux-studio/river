# T2 审阅（第 2 轮）

## 被审版本

`main` @ 3ba346c 加上工作区未提交改动，读取时间 2026-09-25 06:10 左右；文件为 `test/thread.test.ts`、`test/agents.test.ts`。

## 结论

**通过**。

检查命令：两个 tsc 都通过；`npx vitest run` 130/130 通过。

## 前轮处理

| 前轮 | 处理 | 判断 |
|---|---|---|
| F1 三处约定没有断言 | 补了 `pushTool(1, …)` 返回 false；`toolCallId === 'h1-1'`；过期的 `setSummary(3, …)` 传入回调后断言 `seen` 仍为 `[3]`、`hand` 仍为 4 | 已解决。三处都能区分对错：去掉 pushTool 的守卫、改掉 id 格式、或者让过期的 setSummary 走 `enter()`，都会让测试失败 |
| F2 历史测试绑定了教练接口 | 改为用 `opponentTalk` 加上 `Thread` 产出的 messages 来验证，断言角色顺序以及 tool-call、tool-result 的类型 | 已解决，与教练接口解耦 |

## 新发现

无。

## 检查限制

没有用真实模型验证历史里带 tool-call 的兼容性，这一项留给 T7。
