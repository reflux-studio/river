# T4 审阅（第 2 轮）

## 被审版本

`main` @ 3ba346c 加上工作区未提交改动，读取时间 2026-09-25 06:10 左右；文件为 `src/main/agents/coach.ts`、`src/main/table/runner.ts`（教练部分）、`test/runner.test.ts`「教练 thread」。

## 结论

**需修改（只需补一步断言）**。实现方面的四条修复都正确。「无决策点」这条用例只测了复盘的**输入**，task 验证里写的「之后在同一手提问，第一组以【往手回顾】开头」没有测，而前轮 F1 担心的正是**写入**这一侧。

检查命令：两个 tsc 都通过；`npx vitest run` 130/130 通过。

## 前轮处理

| 前轮 | 处理 | 判断 |
|---|---|---|
| F1 无决策点那一手的复盘带往手回顾 | 新增用例：对手全弃，一直驱动到某次 recap 的 messages 只有一条 user、并且以【往手回顾】开头 | **部分解决**。读这一侧能区分对错。写这一侧没有测：如果把 `pushTool(hand, messages.at(-1)…)` 改成写 `heroHandSummary(...)` 原文，测试照样通过，但之后在同一手提问时往手回顾会丢失，这正是前轮点名的风险。建议在同一条用例里 `ask` 一次，断言 `asks[0][0].content` 以【往手回顾】开头、`asks[0][1]` 是 recap 的 tool-call |
| F2 「局面变了就附上」只测了反向 | 新增用例：玩家行动后再提问，断言含「当前局面（玩家视角）」 | 已解决；改成「永远不附」会失败。`chatSeen` 和 `lastSituation` 在失败时不推进、提问失败时写入「（被打断）」这几项仍然没有测试（低，可以留着） |
| F3 空文本写入空 assistant | `writeCoach` 改成 `if (!text) return false` | 已解决 |
| F4 丢了「不论输赢都要复盘」 | `COACH_ASKS.recap` 补上了这句，只改了 user 这一侧 | 已解决 |
| F5 digest 丢了「（被打断）」 | 改成 `text.slice(0, 60) + mark` | 已解决 |
| F6 `SPEAK_ASK` 的命名 | 改名为 `COACH_ASKS`，已经没有旧名残留 | 已解决 |

## 新发现

无。

## 检查限制

没有实机验证，这部分属于 T7。
