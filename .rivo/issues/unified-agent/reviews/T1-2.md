# T1 审阅（第 2 轮）

## 被审版本

`main` @ 3ba346c 加上工作区未提交改动，读取时间 2026-09-25 06:10 左右。

## 结论

**通过**。第 1 轮的两条中等发现都已实质解决，剩下的只有低严重度的建议，不阻塞。

检查命令（本人实跑）：`tsc -p tsconfig.node.json` 通过；`tsc -p tsconfig.web.json` 通过；`npx vitest run` 8 个文件、130 条全部通过。

## 前轮处理

| 前轮 | 处理 | 判断 |
|---|---|---|
| F1 改进牌例子区分不出新旧口径 | 加了 `n('7s 7d','Kc 9h 2s') === 2`（旧口径是 11） | 已解决，能区分新旧口径 |
| F2 runner 层缺断言 | 「对手 thread」第一条用例对 `calls[0]` 的观察断言了「你的位置：」「当前牌型：」、`- 玩家（位置`，以及不含「胜率约」 | 基本解决。对手接线已经测到（座位没改名会失败）。「所需胜率」没有断言；教练一侧（`heroSituation` 重新带上 extra）也没有测到。task 写的是「coachSpeak **或** opponentAct」，所以可以接受 |
| F3 设置页写的是「出路」 | 已改为「改进牌」 | 已解决 |
| F4 `outs` 注释 | 已改成与定义一致 | 已解决 |
| F5 ProbPanel 统计区 3 项加胜率大字 | 实现者说明这是有意设计 | 可以接受，但 task.md 第 95 行仍写着「统计区显示 4 项」，建议在 task 里补一句说明，免得以后按字面返工 |
| F6 没有底牌时抛异常；弃牌后仍给所需胜率 | 没有底牌时牌型显示「—」；弃牌后不再输出所需胜率 | 已解决。这两个分支都没有测试（低）。全下的座位仍会显示所需胜率，前轮建议的「未全下」这一条没有做（低） |
| F7 `cards('')` | `n()` 在公共牌为空时传 `[]` | 已解决 |

## 新发现

- **低**：`situation` 的 `o.extra` 参数在 `src` 里已经没有调用方（`heroSituation` 删掉了 extra，对手也不再传），第 102 行 `if (o.extra)` 成了死代码。建议把参数和这一行一起删掉。

## 检查限制

- 没有启动应用，没有看界面渲染。
- 工作区里 T2–T4 的改动交织在一起，本报告只核对 T1 相关的部分。
